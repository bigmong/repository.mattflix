
.env
*.env
secrets/
*.log
__pycache__/

# Generated by repo_generator.py. These accumulate on the GitHub mirror (one commit per
# publish, so each zip keeps its own date/message) -- Gitea holds only the source.
repo/zips/
omega/zips/
